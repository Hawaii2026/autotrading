# Research + tooling dependencies. Install into a venv:
#   python -m venv .venv && . .venv/bin/activate   (Windows: .venv\Scripts\activate)
#   pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pyarrow>=14.0          # parquet support for research/data
python-dotenv>=1.0     # loads .env for the alert sender
requests>=2.31         # Telegram/Discord POST

# Optional research libraries (uncomment if you want them):
# vectorbt>=0.26
# backtesting>=0.3.3
