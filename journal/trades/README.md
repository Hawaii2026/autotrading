# journal/trades — exported executions & screenshots

Drop NT8 execution / Trade Performance CSV exports and trade screenshots here after
each session. `dashboard/build_data.py` parses the CSVs into the dashboard. Actual
exports are gitignored (private); only this README and `.gitkeep` are tracked.
